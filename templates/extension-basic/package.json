{
  "name": "sqlgui-extension-basic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A basic SQL GUI extension template",
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sqlgui/api": "workspace:*"
  },
  "devDependencies": {
    "@sqlgui/tsconfig": "workspace:*",
    "typescript": "^6.0.3",
    "vite": "^6.0.0"
  }
}
